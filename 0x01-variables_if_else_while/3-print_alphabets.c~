#include <stdio.h>

/**
 * main - entry point
 * Return: 0
 */
int main(void)
{
  char i = 'a';

	while (i <= 'z')
	{
		putchar(i);
		i++;
	}
	char j = 'A';
	while (j <= 'Z')
	{
		putchar(j);
		j++;
	}
	putchar('\n');

	return (0);
}
